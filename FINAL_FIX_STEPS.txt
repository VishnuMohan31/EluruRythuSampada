FINAL FIX - Complete Steps to Resolve All Errors
================================================

PROBLEM: API calls using HTTP instead of HTTPS, causing CSP violations

ROOT CAUSE: Frontend JavaScript files need to be rebuilt with the updated code

SOLUTION: Follow these steps EXACTLY in order:

STEP 1: On your LOCAL machine (Windows)
----------------------------------------
cd D:\Tribal_Products_Website

# Verify the changes are correct
git status
git diff frontend/src/utils/api.js
git diff frontend/.env.production

# Commit and push ALL changes
git add frontend/src/utils/api.js frontend/.env.production frontend/src/pages/public/HomePage.jsx
git commit -m "Final fix: Force relative URLs in production build"
git push origin main


STEP 2: On PRODUCTION server (62.171.191.132)
----------------------------------------------
cd /opt/SwayamEluruConnect

# Pull latest code
git pull origin main

# Verify the api.js file has the new code
grep "import.meta.env.PROD" frontend/src/utils/api.js

# Should output: const API_BASE_URL = import.meta.env.PROD ? '' : (import.meta.env.VITE_API_URL || '')

# Clean old build completely
rm -rf frontend/dist
rm -rf frontend/node_modules/.vite

# Rebuild frontend
cd frontend
npm run build

# Verify build completed
ls -lh dist/
ls -lh dist/assets/

# Check that build doesn't have http://swayam
grep -r "http://swayam" dist/ || echo "✓ Build is clean"

# Go back to project root
cd /opt/SwayamEluruConnect

# Restart nginx to serve new files
sudo docker-compose -f docker-compose.prod.yml restart nginx

# Wait for nginx to start
sleep 5

# Verify nginx has the new files
docker-compose -f docker-compose.prod.yml exec nginx ls -la /usr/share/nginx/html/


STEP 3: In your BROWSER
------------------------
1. Close ALL tabs of swayameluruconnect.in
2. Press Ctrl + Shift + Delete
3. Select "All time"
4. Check ALL boxes (cookies, cache, everything)
5. Click "Clear data"
6. Close browser completely
7. Reopen browser
8. Open INCOGNITO window (Ctrl + Shift + N)
9. Visit: https://swayameluruconnect.in
10. Press F12 to open console
11. Check for errors


EXPECTED RESULT:
----------------
- No CSP violations
- No HTTP errors
- All API calls go to https://swayameluruconnect.in/api/...
- Categories and products load successfully


IF STILL ERRORS:
----------------
Take screenshot of:
1. Browser console (F12 → Console tab)
2. Network tab showing the actual requests
3. Run on server: docker-compose -f docker-compose.prod.yml exec nginx cat /usr/share/nginx/html/index.html

Send all three to verify the issue.
